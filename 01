var foo=1;

var foobbr=foo + bar;

/***** S 行を削除して挿入モード ********/
/***** s カーソル文字を削除して挿入モード ***/
/**** f[char] 指定した文字に移動　; 次の変更対象に移動 ****/
fjafjaljfjslajlfj:

abaaaalllllallllll
jf + ljljflajl var

/*** *カーソルの単語を検索　****/
/*** c　単語を削除して挿入モード　****/
if you are copy wieth this,
we'll lauch as soon as we have the copy

